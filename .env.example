# Environment
NODE_ENV=development
PORT=3000

# Database (PostgreSQL)
# Required for the production persistence adapter (PostgresScanResultRepository).
# Example: DATABASE_URL=postgresql://user:password@localhost:5432/devlens
# When unset, the database is skipped (unit tests use InMemoryScanResultRepository).
DATABASE_URL=
