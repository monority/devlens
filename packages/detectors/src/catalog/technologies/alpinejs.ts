/** Alpine.js — minimal framework (alpinejs script URL). Step 68 declarative catalog. */
import type { TechnologyDefinition } from '../types.js';

export const alpinejs: TechnologyDefinition = {
  id: 'alpinejs',
  name: 'Alpine.js',
  category: 'library',
  scriptUrlSignatures: [{ matchUrl: 'alpinejs', technologyId: 'alpinejs', confidence: 95 }],
};
