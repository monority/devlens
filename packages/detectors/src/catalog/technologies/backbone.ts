/** Backbone.js — framework (inline `Backbone.` content fingerprint). Step 68 declarative catalog. */
import type { TechnologyDefinition } from '../types.js';

export const backbone: TechnologyDefinition = {
  id: 'backbone',
  name: 'Backbone',
  category: 'framework',
  contentSignatures: [{ matchContent: 'backbone.', technologyId: 'backbone', confidence: 95 }],
};
