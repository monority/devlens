/** Ember.js — framework (inline `Ember.` content fingerprint). Step 68 declarative catalog. */
import type { TechnologyDefinition } from '../types.js';

export const ember: TechnologyDefinition = {
  id: 'ember',
  name: 'Ember',
  category: 'framework',
  contentSignatures: [{ matchContent: 'ember.', technologyId: 'ember', confidence: 95 }],
};
