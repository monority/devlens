/** Laravel — framework (inline window.Laravel fingerprint). Step 68 declarative catalog. */
import type { TechnologyDefinition } from '../types.js';

export const laravel: TechnologyDefinition = {
  id: 'laravel',
  name: 'Laravel',
  category: 'framework',
  contentSignatures: [{ matchContent: 'window.Laravel', technologyId: 'laravel', confidence: 90 }],
};
