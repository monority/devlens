/** Popper.js — positioning library (@popperjs script URL). Step 68 declarative catalog. */
import type { TechnologyDefinition } from '../types.js';

export const popperjs: TechnologyDefinition = {
  id: 'popperjs',
  name: 'Popper.js',
  category: 'library',
  scriptUrlSignatures: [{ matchUrl: '@popperjs', technologyId: 'popperjs', confidence: 90 }],
};
