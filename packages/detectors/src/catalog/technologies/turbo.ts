/** Turbo — hypermobility (stimulus-ish) — actually @hotwired/turbo script URL. Step 68 declarative catalog. */
import type { TechnologyDefinition } from '../types.js';

export const turbo: TechnologyDefinition = {
  id: 'turbo',
  name: 'Turbo',
  category: 'library',
  scriptUrlSignatures: [{ matchUrl: '@hotwired/turbo', technologyId: 'turbo', confidence: 95 }],
};
