/** Webflow — CMS (inline Webflow. content fingerprint). Step 68 declarative catalog. */
import type { TechnologyDefinition } from '../types.js';

export const webflow: TechnologyDefinition = {
  id: 'webflow',
  name: 'Webflow',
  category: 'cms',
  contentSignatures: [{ matchContent: 'Webflow.', technologyId: 'webflow', confidence: 90 }],
};
